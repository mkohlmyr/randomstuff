actions = require '../actions.coffee'

React = require 'react'

module.exports = React.createClass {
    getInitialState: ->
    componentDidMount: ->
    componentWillUnmount: ->
    render: () ->
        return null
}
